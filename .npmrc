# React Native's dependency tree has long-standing peer-dependency conflicts
# (React 19, etc.). legacy-peer-deps lets `npm install` resolve cleanly without
# needing extra flags on the command line.
legacy-peer-deps=true
